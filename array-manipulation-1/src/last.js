/* exported last */
function last(array) {
  return array[array.length - 1];
}

// define function last with one argument array
// returns the element that's in the position of the array's length minus 1
