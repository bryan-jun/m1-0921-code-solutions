/* exported setValue */
function setValue(object, key, value) {
  object[key] = value;
}

// define function setValue with three parameters, object, key and value
// set value of object at key key as value
