/* exported getLastChar */

function getLastChar(string) {
  var lastChar = string.length;
  return string[lastChar - 1];
}

// define a function named getLastChar which takes a single parameter of "string"
// define a variable "lastChar" which will be set to the value of the length of string
// return the character at position lastChar - 1 of string
